[project]
name = "ai-daily-brief-worker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tzdata>=2026.2",
]

[dependency-groups]
dev = ["workers-py", "workers-runtime-sdk"]
